import { post, update, delete_ } from '../../../lib/railsclient'





//    return function (dispatch ) => {

     //console.log("addTransactionToDB ", item_name );
    // const payload = {item: {item_name, qty}};
     //delete payload.item.id;
     //console.log("Payload", payload);
  //   post("/transactions", payload, getState().auth_token).then(ret => 
  //      dispatch(addTransactionItem(ret.id, ret.item_name, ret.qty))
  //  }
  //   return { type: "addTransactionToDB" }

