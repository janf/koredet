<%= form_for(transaction_type) do |f| %>
  <% if transaction_type.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(transaction_type.errors.count, "error") %> prohibited this transaction_type from being saved:</h2>

      <ul>
      <% transaction_type.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>


  <div class="field">
    <%= f.label :name %>
    <%= f.text_field :name %>
  </div>

  <div class="field">
    <%= f.label :description %>
    <%= f.text_field :description %>
  </div>

  <div class="field">
    <%= f.label :from_location %>
    <%= f.collection_select(:from_location_id, Location.all, :id, :name) %>  
  </div>
  
  <div class="field">
    <%= f.label :to_location %>
    <%= f.collection_select(:to_location_id, Location.all, :id, :name) %>  
  </div>
  
  <div class="actions">

    <%= f.submit %>
  </div>
<% end %>
