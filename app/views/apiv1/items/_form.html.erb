<%= form_for(apiv1_item) do |f| %>
  <% if apiv1_item.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(apiv1_item.errors.count, "error") %> prohibited this apiv1_item from being saved:</h2>

      <ul>
      <% apiv1_item.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
