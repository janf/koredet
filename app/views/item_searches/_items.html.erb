<div class="item">
  <h2><%= link_to item.name, item %></h2>
  <div class="details">
    <%= item.description) %>
    <% if item.category %>
      | Item Type: <%= item.item_type.name %>
    <% end %>
  </div>
</div>